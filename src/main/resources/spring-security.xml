<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:sec="http://www.springframework.org/schema/security"
         xmlns:security="http://www.springframework.org/schema/security"
         xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">
    <http auto-config="true">
        <intercept-url pattern="/api/**" access="permitAll"/>
        <intercept-url pattern="/login**" access="permitAll"/>
        <intercept-url pattern="/login?*" access="permitAll"/>
        <intercept-url pattern="/**" access="hasAnyRole('ROLE_ADMIN')"/>
        <!--<custom-filter ref="customFilter" after="BASIC_AUTH_FILTER" />-->
        <!--<intercept-url pattern="/admin**" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')"/>-->
        <form-login login-page="/login"
                    default-target-url="/admin"
                    authentication-failure-url="/login?error"
                    login-processing-url="/login"
                    username-parameter="username"
                    password-parameter="password"/>
        <logout logout-success-url="/login?logout"
                logout-url="/j_spring_security_logout"/>
        <!-- enable csrf protection -->
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="anhvv" password="{noop}123456" authorities="ROLE_USER,ROLE_ADMIN" />
            </user-service>
        </authentication-provider>
        <authentication-provider user-service-ref="customUserServiceDetail">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

    <b:bean name="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
    <b:bean name="customUserServiceDetail" class="com.higgsup.hibernate.oauth.CustomUserDetailDAO"/>
    <b:bean name="customFilter" class="com.higgsup.hibernate.config.CustomFilter"/>
</b:beans>